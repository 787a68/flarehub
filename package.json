{
  "name": "flarehub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "npx --yes wrangler@latest dev",
    "deploy": "npx --yes wrangler@latest deploy",
    "check": "node --check src/worker.js && node --check src/access.js && node --check src/github.js && node --check src/http.js && node --check src/registry.js && node --check scripts/prepare-deploy.mjs && node --check public/app.js"
  }
}
